# aiwebsite_generator
An AI-powered web application that generates complete HTML websites from user prompts. Uses an open-source LLaMA 2 model running locally via Ollama, with a FastAPI backend, live frontend preview, and robust output sanitization for reliable generation.
